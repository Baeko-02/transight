services:
  transight-onchain-lab:
    build:
      context: .
      args:
        SITE_URL: ${SITE_URL:-http://localhost:3000}
    environment:
      PORT: 3000
      SITE_URL: ${SITE_URL:-http://localhost:3000}
    ports:
      - "3000:3000"
    restart: unless-stopped
